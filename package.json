{
  "name": "pilates-react",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "rm -rf dist && vite build && cd dist && git init && git checkout -b gh-pages && git add -A && git commit -m deploy && git remote add origin https://github.com/liuzhen219/fitflow.git && git push origin gh-pages --force && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "antd-mobile": "^5.42.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.3"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^6.0.1",
    "jsdom": "^29.1.1",
    "typescript": "~5.8.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
